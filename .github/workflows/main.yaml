name: Go CI & Security Checks

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test-and-security:
    name: Run Tests and AppSec
    runs-on: ubuntu-latest

    steps:
      # 1. Clonar el repositorio
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Configurar el entorno de Go con la versión exacta
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.4'
          cache: true

      # 3. Levantar el contenedor de Postgres
      - name: Start Postgres Database
        run: make db-start

      # 4. Esperar utilizando el Healthcheck nativo de tu docker-compose
      - name: Wait for Postgres to be healthy
        run: |
          echo "⏳ Esperando a que el contenedor reporte estado HEALTHY..."
          until [ "$(docker inspect --format='{{json .State.Health.Status}}' govent-postgres)" == "\"healthy\"" ]; do
            sleep 1
          done
          echo "✅ ¡Postgres está 100% operativo!"

      # 5. Limpieza previa y creación de la base de datos
      - name: Create Database
        env:
          PGHOST: 127.0.0.1
          PGUSER: admin
          PGPASSWORD: admin
        run: |
          docker exec -i govent-postgres psql -U admin -d postgres -c "DROP DATABASE IF EXISTS govent;"
          make db-create

      # 6. Arrancar el API en segundo plano para forzar la creación de tablas
      - name: Start API Server in Background (Triggers Auto-Migrations)
        run: |
          make start &
          echo "⏳ Esperando 5 segundos a que el API inicialice y aplique el esquema..."
          sleep 10

      # 7. Lanzar tus tests unitarios y de integración
      - name: Run Unit Tests
        run: make test

      # 8. ◄— NUEVO PASO: Instalar Snyk CLI para evitar el Error 127
      - name: Install Snyk CLI
        run: npm install -g snyk

      # 9. Lanzar tests de seguridad (AppSec) pasando el Secret
      - name: Run Application Security Tests
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: make appsec-test
